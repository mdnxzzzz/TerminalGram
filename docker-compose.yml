version: '3.8'
services:
  terminalgram:
    build:
      context: .
      dockerfile: Dockerfile.bot
    image: terminalgram-bot
    container_name: terminalgram_bot
    restart: always
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - .:/app
